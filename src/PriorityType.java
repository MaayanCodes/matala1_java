package src;

/**
 * Enumeration representing the priority level of a BoardMessage.
 * Includes three priority types: URGENT, REGULAR, and NOT_URGENT.
 */
public enum PriorityType {
    URGENT,
    REGULAR,
    NOT_URGENT
}